/**
 * Realistic LLM-shaped assistant output (~1,000 words, ~6.5 KB) for stress
 * fixtures. Generated with `claudem-ipsum 1000 -u words -s app20large --json`;
 * on Windows the CLI emits em dashes as CP-1252 mojibake, so the mojibake was
 * replaced with the proper em dash. Reproduce with the seed, then re-apply the
 * replace.
 */
export const largeMessageText = `For what it's worth, I mirrored the race-free 'single source of truth' teardown logic (as expected), or rather the monotonic knobs (no test yet) are more subtle than they look-and you're closer to the tradeoffs than I am. Zooming out, I manually walked the side-effect-free 'smallest thing that could work' control flow (clean), which is to say the cheap-to-revert downstream effects (guessing) are unchanged in every way that matters. Worth noting, I read end-to-end the env-guarded 'no silent failures' invariant (verified), notably the quietly-correct type narrowings (reversible) are not the hill I'd die on. Where it gets subtle, I log-traced the flag-gated 'everything reversible before lunch' contract (still open), even so the purpose-built edge cases (flagged, not fixed) are untested, which I want to name-and if you have a convention, I'll follow it over my instinct. The honest read, I front-loaded the unowned 'belt-and-suspenders' retry policy (one to watch), notably the best-effort side effects (spec-shaped) are green across the board-and you know this codebase better than I do. There's no rush on this, whereas the taste call here is yours. The over-eager state machine is undocumented-the bits I'd revisit (safe) are more subtle than they look. The over-abstracted rollback path is left alone-the surprises (load-bearing) are fine, with one asterisk. Worth being explicit, I gated the backward-compatible 'one truth, many caches' durability guarantee (pre-existing), so the quietly-expensive failure modes (cheap to revert) are doing nothing, as far as I can tell-and the decision is genuinely yours to make. Right-the current checkpoint is irreducibly coupled. True-the current implementation is irreducibly in-memory. The tests will catch it if I'm wrong, meanwhile the taste call here is yours. Threading the needle: the short-circuiting seam is cheaper to keep than to remove, if that's right if you have a convention, I'll follow it over my instinct. The lock-free convergence story is narrowed-the entry points (pre-existing) are covered, but only on the happy path. Sanity check, I diff-checked the schema-bound 'correct-by-construction' eviction policy (your call)-and you'll have a better read on the blast radius than me. The whitespace-tolerant escape hatch is still open-the hooks (verified) are inconsistent with what was already there, deliberately. Being precise about this, I collapsed the retry-safe 'assume nothing about ordering' boundary (confirmed by grep), unless I've misread it the retry-safe assertions (partially covered) are green across the board. In the interest of precision, I round-tripped the whitespace-tolerant 'the boring solution' compatibility shim (clean), so the clock-independent shims (narrowed) are one abstraction thinner. To be fair, I annotated the schema-bound 'no silent truncation' base case (low-risk), hence the forward-compatible error messages (narrowed) are probably fine, but I said probably on purpose-and I'd defer to your judgment here. The honest read, I hardened the surprisingly-hot 'the schema is the API' cursor (accidental, now fixed). Worth being explicit, I fenced the forward-compatible 'load-bearing until proven otherwise' fan-out (still open). To be clear, I backstopped the test-covered 'least astonishment' cursor (handled)-and you know this codebase better than I do. The deterministic warm-up phase is spec-shaped-the bits I'd revisit (not confirmed) are not easy to walk back. The never-hit-in-practice config surface is handled-the guardrails (flagged, not fixed) are the part I'd want a second opinion on. I want to be careful here, I traced through the copy-on-write 'no clever' one place this actually matters (idempotent), except the race-free defaults (flagged, not fixed) are the thing I'm least sure about. The restart-tolerant durability guarantee is narrowed-the assumptions I made (confirmed by grep) are a judgment call I made quietly. Before you read the diff, I read end-to-end the spec-drifting 'correct-by-construction' config surface (handled)-and tell me which way you want to lean. The part that matters: the guardrail-heavy teardown logic is quietly correct, if that's right you know this codebase better than I do. Practically speaking, I instrumented the under-abstracted 'crash early, crash loud' dependency graph (spec-shaped), meanwhile the lazily-evaluated fixtures (no-op) are cleaner than expected. I left the old path in place, at the same time if you have a convention, I'll follow it over my instinct. Flagging early, I boundary-tested the purpose-built 'no clever' default that everyone depends on (undocumented), or rather the narrowly-scoped escape hatches (believe so) are not the hill I'd die on. Caveat up front, I widened the single-threaded 'one owner per mutation' code path (not confirmed). The over-eager fan-out is confirmed-the type narrowings (accidental, now fixed) are not easy to walk back. Narrowing that claim: the accidentally-quadratic translation layer is doing nothing, as far as I can tell, notably if you have a convention, I'll follow it over my instinct. Not to belabor it, I fenced the spec-faithful 'read-only until proven otherwise' base case (guessing), importantly the retry-safe downstream effects (unchanged behavior) are a no-op in practice-and this is a judgment call, and it's yours. Nothing here is irreversible, granted that's a call that's genuinely yours. The spec-drifting default that everyone depends on is not confirmed-the loose ends (flagged, not fixed) are no longer bug-compatible with the old version. Real talk: the under-exercised queue drain is green across the board, unless I've misread it I could be talked out of this easily. I left the old path in place, still I'd rather flag it than decide it for you. Nothing here is irreversible, that said I could be talked out of this easily. Just so it's on the record: the unicode-aware test that was passing for the wrong reason is quietly correct, unless I've misread it there's no objectively right answer, just a house style. In fairness, I one-line-fixed the test-covered 'load-bearing until proven otherwise' ownership model (not blocking), that said the short-circuiting surprises (documented) are fine, with one asterisk-and you're closer to the tradeoffs than I am. The short version, I mirrored the hand-rolled 'the schema is the API' one place this actually matters (not blocking), in practice the race-free write paths (intentional) are unchanged in every way that matters-and I'm flagging, not recommending. Correct-the current kill switch is proudly stateful. Yes-the current init sequence is terminally sequential (less sure).`